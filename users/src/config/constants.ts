export enum Events {
  UserCreated = 'user.created',
}

export const TRANSPORT_CONNECTION = 'TRANSPORT_CONNECTION';
export const NOTIFICATIONS_CHANNEL = 'notifications';

export const USERS_CHANNEL = 'users';
